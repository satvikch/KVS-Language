# Evaluating Fibonacci of a number (res)
start
{
	num x = 0;
	num y = 1;
	num res = 0;
	string z = 'Fibonacci series for N = 10 is a follows :';
	disp z;
	while (res < 10)
	{
		num temp = x;
		x = y;
		y = temp + y;
		disp x;
		res = res + 1;
	}
}
terminate
